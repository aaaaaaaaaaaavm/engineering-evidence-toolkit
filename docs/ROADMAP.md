# Roadmap

1. Publish a versioned JSON Schema for `engtrace.json`.
2. Add JUnit and SARIF output without changing the core verdicts.
3. Add a directed source-to-artifact graph after defining cycle behavior.
4. Exercise the tool against a second external computational project.
